__pycache__/
*.py[cod]
.venv/
.env
.ipynb_checkpoints/

# Data files - keep sample files for Docker builds
# Ignore large training data but keep test samples
data/raw/synthetic_iot_data.csv
# Keep smaller test files
!data/raw/test_sample.csv

# Model files - keep production models for Docker builds
# Models directory is needed but keep production artifacts
!models/production/
!models/production/*.pkl
!models/production/*.txt
!models/production/*.json

# Checkpoints and temporary files
checkpoints/
*.tmp
.DS_Store

# MLflow
mlruns/
mlartifacts/